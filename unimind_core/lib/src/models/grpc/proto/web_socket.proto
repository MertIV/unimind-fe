syntax = "proto3";
import "user.proto";

message WebSocketMessage{
    string type = 1;
    string data = 2;
    string operation = 3;
}

message ConnectionRequest{
    User user = 1;
    string connectionCode = 2;
}

service WebSocketService{
    rpc Connect(stream ConnectionRequest) returns (stream WebSocketMessage);
}