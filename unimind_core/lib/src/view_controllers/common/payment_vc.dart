import 'package:unimind_core/unimind_core.dart';
import 'package:unimind_core/src/controllers/_controller_import.dart';
// import 'package:webview_windows/webview_windows.dart';

class PaymentVC extends GetxController {
  // String _url;
  // PaymentVC({String url: "https://flutter.dev"}) {
  //   _url = url;
  // }
  // final controller = WebviewController();

  // Future<void> initPlatformState({Function callback}) async {
  //   // Optionally initialize the webview environment using
  //   // a custom user data directory and/or custom chromium command line flags
  //   //await WebviewController.initializeEnvironment(
  //   //    additionalArguments: '--show-fps-counter');

  //   await controller.initialize();

  //   await controller.setBackgroundColor(Colors.transparent);
  //   await controller.loadUrl(_url);

  //   callback?.call();
  // }
}
